--[[
	Class script: Board
	Module: 	  BattleField
	
	[src]	
	pos
	size
	cellSize
	:setPosition(x,y)
	:setSize(x,y)
	:setCellSize(x,y)
	.instance()
]]
	
-- local Board = BattleField.Board

-- function Board:setStat(stat)
	-- if stat.pos then self:setPosition(stat.pos[1], stat.pos[2]) end
	-- if stat.size then self:setSize(stat.size[1], stat.size[2]) end
	-- if stat.cellSize then self:setCellSize(stat.cellSize[1], stat.cellSize[2]) end
	-- end
