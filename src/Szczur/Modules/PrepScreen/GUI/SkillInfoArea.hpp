#pragma once

#include "BaseBar.hpp"

#include <vector>
#include <string>

#include <SFML/System/Vector2.hpp>

#include "../SkillBaseInfo.hpp"

namespace rat
{
    /*
    class TextAreaWidget; class TextWidget;

    class SkillInfoArea : public BaseBar
    {
    public:
        SkillInfoArea();
    private:
        float _width{100.f};
        float _nameTextHeight{20.f};
        float _iconDim{50.f};
        float _textsPadding{5.f};
        
        std::vector<TextAreaWidget*> _texts;
        size_t _numOfActiveTetxts{0u};
        Widget* _textsBase{nullptr};

        ImageWidget* _mainBar{nullptr};
        TextWidget* _nameText{nullptr};
        ImageWidget* _skillIcon{nullptr};


    };
    */
}