#pragma once

#include <string>
#include <SFML/Graphics/Texture.hpp>


namespace rat
{
    /*
    class Widget; class ImageWidget;
    class SkillArea;

    class ColorBar
    {
    public:
        ColorBar(SkillArea& parentArea);

        void setParent(Widget* parent);
        void setPosition(float x, float y);
        void setIconTexture(sf::Texture* texture);

    private:
        SkillArea& _parentArea;

        std:string color;

        ImageWidget* _icon{nullptr};
        bool _isChosen{false};
        void _onClick();
    };
    */
}