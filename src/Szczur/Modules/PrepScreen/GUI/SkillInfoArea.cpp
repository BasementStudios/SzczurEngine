#include "SkillInfoArea.hpp"

#include "Szczur/Modules/GUI/ImageWidget.hpp"
#include "Szczur/Modules/GUI/TextWidget.hpp"
#include "Szczur/Modules/GUI/TextAreaWidget.hpp"

namespace rat
{
    /*
    SkillInfoArea::SkillInfoArea()
    {
        _textsBase = new Widget;
        _textsBase->setPosition(_iconDim, _nameTextHeight);
        _addWidget(_textsBase);

        _mainBar = new ImageWidget;
        _addWidget(_mainBar);
        _nameText = new TextWidget;
        _addWidget(_nameText);
        _skillIcon = ImageWidget;
        _addWidget(_skillIcon);
    }

    void SkillInfoArea::setInfo(const SkillBaseInfo& info)
    {

    }

  

*/
}