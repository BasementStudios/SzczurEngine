#pragma once
/*
#include "Szczur/Modules/GUI/Base/BaseBar.hpp"

#include <vector>
//#include <unordered_map>

#include <SFML/System/Vector2.hpp>

#include "../SkillBaseInfo.hpp"
*/
namespace rat
{
    /*
    class ImageWidget; class TextWidget; class TextAreaWidget;

    class InfoSkillArea : public BaseBar
    {
    public:
        InfoSkillArea();
        void setSize(float width, float height);
        //void setSkill(Skill* skill);

    private:
        TextWidget* _name{nullptr};
        TextAreaWidget* _description {nullptr};
        TextWidget* _cooldown{nullptr};
        TextWidget* _durationTime{nullptr};
        TextWidget* _areas{nullptr};
        TextAreaWidget* _effect{nullptr};


        Widget* _properties;
        size_t _numOfProps{0};
        std::vector<TextAreaWidget*> _textAreas;

        size_t _fontSize{9};
        size_t _textWidth{50};

        sf::Vector2f _size{100.f, 100.f};


        void _setBaseInfo(SkillBaseInfo& info);
        void _recalculate();
        void _initProperty(TextWidget* property);
        void _initTextArea(TextAreaWidget* text);
    };
    */
}