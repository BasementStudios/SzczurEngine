#include "PreparationScreen.hpp"

namespace rat
{
	PreparationScreen::PreparationScreen(const int& player, const int& battler)
		: _player(player), _battler(battler) {
	}

	bool PreparationScreen::run()
	{

	}
}